void twinkleTwinkleDbMajor() {
  playNoteLength(db5, quarterNote);
  playNoteLength(db5, quarterNote);
  playNoteLength(ab5, quarterNote);
  playNoteLength(ab5, quarterNote);
  playNoteLength(bb5, quarterNote);
  playNoteLength(bb5, quarterNote);
  playNoteLength(ab5, halfNote);
  playNoteLength(gb5, quarterNote);
  playNoteLength(gb5, quarterNote);
  playNoteLength(f5, quarterNote);
  playNoteLength(f5, quarterNote);
  playNoteLength(eb5, quarterNote);
  playNoteLength(eb5, quarterNote);
  playNoteLength(db5, halfNote);
  playNoteLength(ab5, quarterNote);
  playNoteLength(ab5, quarterNote);
  playNoteLength(gb5, quarterNote);
  playNoteLength(gb5, quarterNote);
  playNoteLength(f5, quarterNote);
  playNoteLength(f5, quarterNote);
  playNoteLength(eb5, halfNote);
  playNoteLength(ab5, quarterNote);
  playNoteLength(ab5, quarterNote);
  playNoteLength(gb5, quarterNote);
  playNoteLength(gb5, quarterNote);
  playNoteLength(f5, quarterNote);
  playNoteLength(f5, quarterNote);
  playNoteLength(eb5, halfNote);
  playNoteLength(db5, quarterNote);
  playNoteLength(db5, quarterNote);
  playNoteLength(ab5, quarterNote);
  playNoteLength(ab5, quarterNote);
  playNoteLength(bb5, quarterNote);
  playNoteLength(bb5, quarterNote);
  playNoteLength(ab5, halfNote);
  playNoteLength(gb5, quarterNote);
  playNoteLength(gb5, quarterNote);
  playNoteLength(f5, quarterNote);
  playNoteLength(f5, quarterNote);
  playNoteLength(eb5, quarterNote);
  playNoteLength(eb5, quarterNote);
  playNoteLength(db5, wholeNote);
}
